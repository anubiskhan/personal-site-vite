FROM node:20-alpine

WORKDIR /app

# Install dependencies only
COPY package*.json ./
RUN npm install

# Don't copy source code - we'll mount it as a volume

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"] 